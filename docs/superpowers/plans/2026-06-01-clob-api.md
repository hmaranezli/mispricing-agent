# Polymarket CLOB API Entegrasyonu — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mevcut DRY_RUN executor'ın yanına gerçek Polymarket CLOB order placement katmanı ekle; `DRY_RUN=False` iken sistem gerçek order göndersin.

**Architecture:** `execution/clob_executor.py` yeni dosyası `executor.py` ile birebir aynı interface'i uygular. `main_loop.py` tek satır router ile `DRY_RUN` flag'ine göre doğru executor'ı çağırır. Exit (sell) için `execution/position_store.py` token holdings'i yönetir.

**Tech Stack:** Python 3.12, py-clob-client, aiosqlite, pytest, pytest-asyncio, unittest.mock — mevcut stack + py-clob-client.

**Not:** `council/redteam.py` zaten `taker_fee=0.02` kullanıyor — fee kalibrasyon değişikliği gerekmez.

---

## Dosya Haritası

| Dosya | İşlem | Değişiklik |
|-------|--------|-----------|
| `requirements.txt` | Güncelle | `py-clob-client` ekle |
| `db/schema.py` | Güncelle | `_MIGRATIONS`'a 4 yeni ALTER TABLE satırı |
| `db/logger.py` | Güncelle | `log_position_open` + `_load_open_positions` — 4 yeni alan |
| `council/scout.py` | Güncelle | `_process_market` → `yes_token_id`, `no_token_id` finding'e ekle |
| `execution/clob_client.py` | Yeni | `get_client()` singleton, lazy init |
| `execution/clob_executor.py` | Yeni | `execute()` BUY order — executor.py ile aynı interface |
| `execution/position_store.py` | Yeni | `sell_position()` — token sat, fill fiyatını döndür |
| `main_loop.py` | Güncelle | executor router + sell path + BANKROLL_USD env |
| `tests/test_clob_client.py` | Yeni | 3 test |
| `tests/test_clob_executor.py` | Yeni | 4 test |
| `tests/test_position_store.py` | Yeni | 3 test |

---

## Task 1: requirements.txt + py-clob-client kurulumu

**Dosyalar:**
- Güncelle: `requirements.txt`

- [ ] **Step 1: requirements.txt'e ekle**

`requirements.txt` dosyasının sonuna ekle:
```
py-clob-client
```

- [ ] **Step 2: Kur**

```bash
cd /root/mispricing_agent && source venv/bin/activate
pip install py-clob-client
```

Beklenen: başarılı kurulum, `py_clob_client` import edilebilir.

- [ ] **Step 3: Doğrula**

```bash
python -c "from py_clob_client.client import ClobClient; print('OK')"
```

Beklenen: `OK`

- [ ] **Step 4: Commit**

```bash
git add requirements.txt
git commit -m "chore: py-clob-client bağımlılığı eklendi"
```

---

## Task 2: DB Schema — 4 Yeni Kolon + logger güncelleme

**Dosyalar:**
- Güncelle: `db/schema.py`
- Güncelle: `db/logger.py`
- Güncelle: `tests/test_db.py`

### Step 1: Failing test yaz

`tests/test_db.py` sonuna ekle:

```python
@pytest.mark.asyncio
async def test_positions_schema_has_clob_columns(conn):
    """positions tablosu shares, order_id, yes_token_id, no_token_id sütunlarına sahip olmalı."""
    async with conn.execute("PRAGMA table_info(positions)") as cur:
        cols = {row[1] for row in await cur.fetchall()}
    assert "shares"       in cols, "shares sütunu yok"
    assert "order_id"     in cols, "order_id sütunu yok"
    assert "yes_token_id" in cols, "yes_token_id sütunu yok"
    assert "no_token_id"  in cols, "no_token_id sütunu yok"


@pytest.mark.asyncio
async def test_log_position_open_stores_clob_fields(conn):
    """log_position_open shares, order_id, yes_token_id, no_token_id alanlarını yazar."""
    pos = {
        "position_id": "clob-001", "slug": "btc-up-5min", "asset": "BTC",
        "action": "YES", "pm_entry_price": 0.35, "fair_value": 0.55,
        "ref_price": 95000.0, "edge": 0.20,
        "position_usd": 25.0, "kelly_f": 0.15, "confidence_score": 82.0,
        "opened_at": "2026-06-01T10:00:00+00:00",
        "shares": 71.43, "order_id": "ord-abc123",
        "yes_token_id": "yes-tok-111", "no_token_id": "no-tok-222",
    }
    await logger.log_position_open(conn, pos)
    async with conn.execute(
        "SELECT shares, order_id, yes_token_id, no_token_id FROM positions WHERE position_id='clob-001'"
    ) as cur:
        row = await cur.fetchone()
    assert abs(row[0] - 71.43) < 0.01
    assert row[1] == "ord-abc123"
    assert row[2] == "yes-tok-111"
    assert row[3] == "no-tok-222"
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
cd /root/mispricing_agent && source venv/bin/activate
pytest tests/test_db.py::test_positions_schema_has_clob_columns -v
```

Beklenen: FAIL — `shares sütunu yok`

- [ ] **Step 3: db/schema.py'yi güncelle**

`_MIGRATIONS` listesine 4 yeni satır ekle:

```python
_MIGRATIONS = [
    "ALTER TABLE positions ADD COLUMN ref_price REAL",
    "ALTER TABLE positions ADD COLUMN edge REAL",
    "ALTER TABLE positions ADD COLUMN realized_pnl REAL",
    # CLOB API kolonları:
    "ALTER TABLE positions ADD COLUMN shares REAL",
    "ALTER TABLE positions ADD COLUMN order_id TEXT",
    "ALTER TABLE positions ADD COLUMN yes_token_id TEXT",
    "ALTER TABLE positions ADD COLUMN no_token_id TEXT",
]
```

- [ ] **Step 4: db/logger.py — log_position_open güncelle**

`log_position_open` içindeki INSERT sorgusunu güncelle.

Mevcut INSERT:
```python
    await conn.execute(
        """INSERT OR IGNORE INTO positions
               (position_id, ts_open, slug, asset, action, pm_entry_price,
                fair_value, ref_price, edge, position_usd, kelly_f,
                confidence_score, status, dry_run)
           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'open', ?)""",
        (
            position["position_id"],
            position.get("opened_at", datetime.now(timezone.utc).isoformat()),
            position.get("slug", ""),
            position.get("asset", ""),
            position.get("action", ""),
            position.get("pm_entry_price"),
            position.get("fair_value"),
            position.get("ref_price"),
            position.get("edge"),
            position.get("position_usd"),
            position.get("kelly_f"),
            position.get("confidence_score"),
            1 if config.DRY_RUN else 0,
        ),
    )
```

Yeni hali (sütun listesi + değerler genişletildi):
```python
    await conn.execute(
        """INSERT OR IGNORE INTO positions
               (position_id, ts_open, slug, asset, action, pm_entry_price,
                fair_value, ref_price, edge, position_usd, kelly_f,
                confidence_score, status, dry_run,
                shares, order_id, yes_token_id, no_token_id)
           VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'open', ?, ?, ?, ?, ?)""",
        (
            position["position_id"],
            position.get("opened_at", datetime.now(timezone.utc).isoformat()),
            position.get("slug", ""),
            position.get("asset", ""),
            position.get("action", ""),
            position.get("pm_entry_price"),
            position.get("fair_value"),
            position.get("ref_price"),
            position.get("edge"),
            position.get("position_usd"),
            position.get("kelly_f"),
            position.get("confidence_score"),
            1 if config.DRY_RUN else 0,
            position.get("shares"),
            position.get("order_id"),
            position.get("yes_token_id"),
            position.get("no_token_id"),
        ),
    )
```

- [ ] **Step 5: db/logger.py — _load_open_positions güncelle (main_loop.py içinde)**

`main_loop.py`'deki `_load_open_positions` SELECT sorgusunu ve dict yapısını güncelle.

Mevcut SELECT:
```python
    async with conn.execute(
        "SELECT position_id, ts_open, slug, asset, action, pm_entry_price, "
        "fair_value, ref_price, edge, position_usd, kelly_f, confidence_score, dry_run "
        "FROM positions WHERE status='open'"
    ) as cur:
```

Yeni SELECT (4 yeni alan eklendi, r indeksleri 13-16 arası):
```python
    async with conn.execute(
        "SELECT position_id, ts_open, slug, asset, action, pm_entry_price, "
        "fair_value, ref_price, edge, position_usd, kelly_f, confidence_score, dry_run, "
        "shares, order_id, yes_token_id, no_token_id "
        "FROM positions WHERE status='open'"
    ) as cur:
```

Ve dict yapısı — mevcut alanlara ek olarak:
```python
        return [
            {
                "position_id":            r[0],
                "opened_at":              r[1],
                "slug":                   r[2],
                "asset":                  r[3],
                "action":                 r[4],
                "pm_entry_price":         r[5],
                "fair_value":             r[6],
                "ref_price":              r[7],
                "edge":                   r[8],
                "position_usd":           r[9],
                "kelly_f":                r[10],
                "confidence_score":       r[11],
                "status":                 "open",
                "requires_human_approval": (r[9] or 0) > config.HUMAN_APPROVAL_USD,
                "dry_run":                bool(r[12]),
                "exit_reason":            None,
                "closed_at":              None,
                "shares":                 r[13],
                "order_id":               r[14],
                "yes_token_id":           r[15],
                "no_token_id":            r[16],
            }
            for r in rows
        ]
```

- [ ] **Step 6: Testleri çalıştır — PASS bekliyoruz**

```bash
pytest tests/test_db.py::test_positions_schema_has_clob_columns \
       tests/test_db.py::test_log_position_open_stores_clob_fields -v
```

Beklenen: 2 PASSED

- [ ] **Step 7: Tam test suite — regresyon yok mu?**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 2 yeni PASSED, 0 FAILED

- [ ] **Step 8: Commit**

```bash
git add db/schema.py db/logger.py main_loop.py tests/test_db.py
git commit -m "feat(db): CLOB kolonları — shares, order_id, yes/no_token_id"
```

---

## Task 3: council/scout.py — yes_token_id + no_token_id finding'e ekle

**Dosyalar:**
- Güncelle: `council/scout.py`
- Güncelle: `tests/test_scout.py` (varsa)

- [ ] **Step 1: Failing test yaz**

`tests/test_scout.py` dosyasını bul. Eğer yoksa `tests/test_scout.py` oluştur:

```python
"""tests/test_scout.py — scout modülü için testler (token ID odaklı)."""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import pytest
from unittest.mock import AsyncMock, patch


@pytest.mark.asyncio
async def test_finding_contains_token_ids():
    """scan_edges() döndürdüğü finding'de yes_token_id ve no_token_id bulunur."""
    fake_market = {
        "question": "Will BTC go up?",
        "slug": "btc-updown-5m-123",
        "bestAsk": "0.35",
        "bestBid": "0.33",
        "eventStartTime": "2026-06-01T10:00:00Z",
        "endDate": "2026-06-01T10:05:00Z",
        "negRisk": False,
        "clobTokenIds": ["yes-token-abc", "no-token-xyz"],
    }
    with patch("council.scout.find_shortterm", new_callable=AsyncMock) as mock_find, \
         patch("council.scout.price_at_timestamp", new_callable=AsyncMock) as mock_ref, \
         patch("council.scout.current_price", new_callable=AsyncMock) as mock_cur, \
         patch("council.scout.fair_yes", return_value=0.60):
        mock_find.return_value = [fake_market]
        mock_ref.return_value = 95000.0
        mock_cur.return_value = 96000.0
        findings = await __import__("council.scout", fromlist=["scan_edges"]).scan_edges()

    assert len(findings) >= 1
    f = findings[0]
    assert f.get("yes_token_id") == "yes-token-abc", f"yes_token_id eksik veya yanlış: {f}"
    assert f.get("no_token_id")  == "no-token-xyz",  f"no_token_id eksik veya yanlış: {f}"


@pytest.mark.asyncio
async def test_finding_token_ids_none_when_absent():
    """clobTokenIds yoksa yes_token_id ve no_token_id None döner, hata vermez."""
    fake_market = {
        "question": "Will BTC go up?",
        "slug": "btc-updown-5m-456",
        "bestAsk": "0.35",
        "bestBid": "0.33",
        "eventStartTime": "2026-06-01T10:00:00Z",
        "endDate": "2026-06-01T10:05:00Z",
        "negRisk": False,
        # clobTokenIds YOK
    }
    with patch("council.scout.find_shortterm", new_callable=AsyncMock) as mock_find, \
         patch("council.scout.price_at_timestamp", new_callable=AsyncMock) as mock_ref, \
         patch("council.scout.current_price", new_callable=AsyncMock) as mock_cur, \
         patch("council.scout.fair_yes", return_value=0.60):
        mock_find.return_value = [fake_market]
        mock_ref.return_value = 95000.0
        mock_cur.return_value = 96000.0
        findings = await __import__("council.scout", fromlist=["scan_edges"]).scan_edges()

    if findings:
        assert findings[0].get("yes_token_id") is None
        assert findings[0].get("no_token_id")  is None
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
pytest tests/test_scout.py::test_finding_contains_token_ids -v
```

Beklenen: FAIL — `yes_token_id eksik`

- [ ] **Step 3: council/scout.py — _process_market güncelle**

`_process_market` fonksiyonunun `return` bloğuna 2 satır ekle.

Mevcut return (son 3 satır):
```python
        "slug":              m.get("slug", ""),
        "_window":           window,
        "_raw_market":       m,
    }
```

Yeni hali:
```python
        "slug":              m.get("slug", ""),
        "_window":           window,
        "_raw_market":       m,
        "yes_token_id":      (m.get("clobTokenIds") or [None, None])[0],
        "no_token_id":       (m.get("clobTokenIds") or [None, None])[1],
    }
```

- [ ] **Step 4: Testleri çalıştır — 2 PASS bekliyoruz**

```bash
pytest tests/test_scout.py::test_finding_contains_token_ids \
       tests/test_scout.py::test_finding_token_ids_none_when_absent -v
```

Beklenen: 2 PASSED

- [ ] **Step 5: Tam test suite**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 2 yeni PASSED, 0 FAILED

- [ ] **Step 6: Commit**

```bash
git add council/scout.py tests/test_scout.py
git commit -m "feat(scout): yes_token_id + no_token_id finding'e eklendi"
```

---

## Task 4: execution/clob_client.py — Singleton

**Dosyalar:**
- Yeni: `execution/clob_client.py`
- Yeni: `tests/test_clob_client.py`

- [ ] **Step 1: Failing testleri yaz**

`tests/test_clob_client.py` oluştur:

```python
"""tests/test_clob_client.py — clob_client singleton testleri."""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import pytest
from unittest.mock import patch, MagicMock


def test_get_client_raises_when_env_missing(monkeypatch):
    """POLY_PRIVATE_KEY yoksa get_client() KeyError verir."""
    from execution import clob_client as cc
    cc.reset_client()
    for key in ("POLY_PRIVATE_KEY", "POLY_API_KEY", "POLY_API_SECRET", "POLY_API_PASSPHRASE"):
        monkeypatch.delenv(key, raising=False)
    with pytest.raises((KeyError, Exception)):
        cc.get_client()
    cc.reset_client()


def test_get_client_returns_singleton(monkeypatch):
    """get_client() iki kez çağrılınca aynı nesneyi döndürür."""
    from execution import clob_client as cc
    cc.reset_client()
    monkeypatch.setenv("POLY_PRIVATE_KEY",    "0xdeadbeef")
    monkeypatch.setenv("POLY_API_KEY",        "test-key")
    monkeypatch.setenv("POLY_API_SECRET",     "test-secret")
    monkeypatch.setenv("POLY_API_PASSPHRASE", "test-pass")
    monkeypatch.setenv("POLY_WALLET_ADDRESS", "0xabc")

    fake_client = MagicMock()
    with patch("execution.clob_client.ClobClient", return_value=fake_client):
        c1 = cc.get_client()
        c2 = cc.get_client()
    assert c1 is c2
    cc.reset_client()


def test_reset_client_clears_singleton(monkeypatch):
    """reset_client() sonrası get_client() yeni nesne oluşturur."""
    from execution import clob_client as cc
    cc.reset_client()
    monkeypatch.setenv("POLY_PRIVATE_KEY",    "0xdeadbeef")
    monkeypatch.setenv("POLY_API_KEY",        "test-key")
    monkeypatch.setenv("POLY_API_SECRET",     "test-secret")
    monkeypatch.setenv("POLY_API_PASSPHRASE", "test-pass")
    monkeypatch.setenv("POLY_WALLET_ADDRESS", "0xabc")

    fake1 = MagicMock()
    fake2 = MagicMock()
    with patch("execution.clob_client.ClobClient", side_effect=[fake1, fake2]):
        c1 = cc.get_client()
        cc.reset_client()
        c2 = cc.get_client()
    assert c1 is not c2
    cc.reset_client()
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
pytest tests/test_clob_client.py -v
```

Beklenen: `ImportError: cannot import name 'clob_client'`

- [ ] **Step 3: execution/clob_client.py oluştur**

```python
"""execution/clob_client.py — py-clob-client singleton. Lazy init, env'den credentials."""
import os
from py_clob_client.client import ClobClient
from py_clob_client.clob_types import ApiCreds

POLY_HOST = "https://clob.polymarket.com"
POLYGON_CHAIN_ID = 137

_client: ClobClient | None = None


def get_client() -> ClobClient:
    """Singleton ClobClient döndür. İlk çağrıda oluşturulur."""
    global _client
    if _client is None:
        _client = ClobClient(
            host=POLY_HOST,
            key=os.environ["POLY_PRIVATE_KEY"],
            chain_id=POLYGON_CHAIN_ID,
            creds=ApiCreds(
                api_key=os.environ["POLY_API_KEY"],
                api_secret=os.environ["POLY_API_SECRET"],
                api_passphrase=os.environ["POLY_API_PASSPHRASE"],
            ),
        )
    return _client


def reset_client() -> None:
    """Global state'i sıfırla — test yardımcısı."""
    global _client
    _client = None
```

- [ ] **Step 4: Testleri çalıştır — 3 PASS bekliyoruz**

```bash
pytest tests/test_clob_client.py -v
```

Beklenen: 3 PASSED

- [ ] **Step 5: Tam test suite**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 3 yeni PASSED, 0 FAILED

- [ ] **Step 6: Commit**

```bash
git add execution/clob_client.py tests/test_clob_client.py
git commit -m "feat(execution): clob_client singleton — py-clob-client wrapper"
```

---

## Task 5: execution/clob_executor.py — BUY order

**Dosyalar:**
- Yeni: `execution/clob_executor.py`
- Yeni: `tests/test_clob_executor.py`

- [ ] **Step 1: Failing testleri yaz**

`tests/test_clob_executor.py` oluştur:

```python
"""tests/test_clob_executor.py — clob_executor execute() testleri."""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import pytest
from unittest.mock import patch, MagicMock


def _finding(action="YES"):
    return {
        "question": "Will BTC go up?", "asset": "BTC", "action": action,
        "fair_value": 0.55, "ref_price": 95000.0, "cur_price": 96000.0,
        "best_ask": 0.35, "best_bid": 0.33, "seconds_remaining": 900,
        "edge": 0.20, "slug": "btc-up-5m-test", "neg_risk": False,
        "yes_token_id": "yes-tok-111", "no_token_id": "no-tok-222",
    }

def _gate():
    return {"pass": True, "confidence_score": 82.5, "action_taken": "open"}

def _risk():
    return {"pass": True, "position_usd": 25.0, "kelly_f": 0.15,
            "kelly_fraction_applied": 0.25, "reason": ""}

def _mock_matched_order(price="0.35", size_filled="71.43", order_id="ord-abc"):
    order = MagicMock()
    order.__getitem__ = lambda self, k: {
        "status": "MATCHED", "orderID": order_id,
        "sizeFilled": size_filled, "price": price,
    }[k]
    order.get = lambda k, d=None: {
        "status": "MATCHED", "orderID": order_id,
        "sizeFilled": size_filled, "price": price,
    }.get(k, d)
    return order


@pytest.mark.asyncio
async def test_execute_returns_position_on_matched_order():
    """Order MATCHED → position dict döner, gerekli alanlar dolu."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "orderID": "ord-abc",
        "sizeFilled": "71.43", "price": "0.35",
    }
    with patch("execution.clob_executor.get_client", return_value=fake_client):
        from execution.clob_executor import execute
        result = await execute(_finding("YES"), _gate(), _risk(), [])

    assert result is not None
    assert result["asset"] == "BTC"
    assert result["action"] == "YES"
    assert result["yes_token_id"] == "yes-tok-111"
    assert abs(result["shares"] - 71.43) < 0.01
    assert result["order_id"] == "ord-abc"
    assert result["dry_run"] is False


@pytest.mark.asyncio
async def test_execute_returns_none_when_not_matched():
    """Order UNMATCHED → None döner, pozisyon açılmaz."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "UNMATCHED", "orderID": "ord-xyz", "sizeFilled": "0", "price": "0.35",
    }
    with patch("execution.clob_executor.get_client", return_value=fake_client):
        from execution.clob_executor import execute
        result = await execute(_finding("YES"), _gate(), _risk(), [])
    assert result is None


@pytest.mark.asyncio
async def test_execute_uses_yes_token_for_yes_action():
    """YES action → yes_token_id order'a gönderilir."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "orderID": "x", "sizeFilled": "71.0", "price": "0.35",
    }
    with patch("execution.clob_executor.get_client", return_value=fake_client):
        from execution.clob_executor import execute
        await execute(_finding("YES"), _gate(), _risk(), [])
    call_kwargs = fake_client.create_and_post_order.call_args
    order_args = call_kwargs[0][0] if call_kwargs[0] else call_kwargs[1]
    token_used = order_args.get("token_id") if isinstance(order_args, dict) else getattr(order_args, "token_id", None)
    assert token_used == "yes-tok-111"


@pytest.mark.asyncio
async def test_execute_uses_no_token_for_no_action():
    """NO action → no_token_id order'a gönderilir."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "orderID": "x", "sizeFilled": "71.0", "price": "0.67",
    }
    with patch("execution.clob_executor.get_client", return_value=fake_client):
        from execution.clob_executor import execute
        await execute(_finding("NO"), _gate(), _risk(), [])
    call_kwargs = fake_client.create_and_post_order.call_args
    order_args = call_kwargs[0][0] if call_kwargs[0] else call_kwargs[1]
    token_used = order_args.get("token_id") if isinstance(order_args, dict) else getattr(order_args, "token_id", None)
    assert token_used == "no-tok-222"
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
pytest tests/test_clob_executor.py -v
```

Beklenen: `ImportError: cannot import name 'execute'`

- [ ] **Step 3: execution/clob_executor.py oluştur**

```python
"""execution/clob_executor.py — Gerçek Polymarket CLOB order placement.

executor.py ile birebir aynı interface: execute(finding, gate, risk, open_pos) -> dict | None
"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from datetime import datetime, timezone
from uuid import uuid4
import config
from execution.clob_client import get_client


async def execute(
    finding:        dict,
    gate_result:    dict,
    risk_result:    dict,
    open_positions: list[dict],
) -> dict | None:
    """Polymarket CLOB'a BUY order gönder. Dolarsa position dict döner, dolmazsa None."""
    action    = finding["action"]
    token_id  = finding["yes_token_id"] if action == "YES" else finding["no_token_id"]

    if not token_id:
        print(f"[clob] {finding['slug']}: token_id yok, order gönderilmedi")
        return None

    position_usd = risk_result["position_usd"]
    entry_price  = finding["best_ask"]
    if entry_price <= 0:
        return None
    shares = round(position_usd / entry_price, 4)

    client = get_client()
    order_args = {
        "token_id":      token_id,
        "price":         entry_price,
        "size":          shares,
        "side":          "BUY",
        "time_in_force": "IOC",   # Immediate Or Cancel — hemen dolarsa al, dolmazsa iptal
    }

    try:
        resp = client.create_and_post_order(order_args)
    except Exception as e:
        print(f"[clob] {finding['slug']}: order hatası — {e}")
        return None

    if not resp:
        return None

    # py-clob-client dict veya object döndürebilir — her ikisini de destekle
    status       = resp.get("status") if isinstance(resp, dict) else getattr(resp, "status", "")
    order_id     = resp.get("orderID") if isinstance(resp, dict) else getattr(resp, "orderID", "")
    size_filled  = resp.get("sizeFilled") if isinstance(resp, dict) else getattr(resp, "sizeFilled", "0")
    fill_price_s = resp.get("price") if isinstance(resp, dict) else getattr(resp, "price", str(entry_price))

    if status != "MATCHED":
        print(f"[clob] {finding['slug']}: order UNMATCHED (status={status})")
        return None

    fill_price  = float(fill_price_s or entry_price)
    fill_shares = float(size_filled or shares)

    return {
        "position_id":             str(uuid4()),
        "asset":                   finding["asset"],
        "action":                  action,
        "slug":                    finding["slug"],
        "pm_entry_price":          fill_price,
        "fair_value":              finding["fair_value"],
        "ref_price":               finding["ref_price"],
        "edge":                    finding["edge"],
        "position_usd":            fill_price * fill_shares,
        "kelly_f":                 risk_result["kelly_f"],
        "confidence_score":        gate_result["confidence_score"],
        "shares":                  fill_shares,
        "order_id":                order_id,
        "fill_price":              fill_price,
        "yes_token_id":            finding.get("yes_token_id"),
        "no_token_id":             finding.get("no_token_id"),
        "requires_human_approval": False,
        "dry_run":                 False,
        "status":                  "open",
        "opened_at":               datetime.now(timezone.utc).isoformat(),
    }
```

- [ ] **Step 4: Testleri çalıştır — 4 PASS bekliyoruz**

```bash
pytest tests/test_clob_executor.py -v
```

Beklenen: 4 PASSED

- [ ] **Step 5: Tam test suite**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 4 yeni PASSED, 0 FAILED

- [ ] **Step 6: Commit**

```bash
git add execution/clob_executor.py tests/test_clob_executor.py
git commit -m "feat(execution): clob_executor — BUY order placement, IOC"
```

---

## Task 6: execution/position_store.py — SELL order

**Dosyalar:**
- Yeni: `execution/position_store.py`
- Yeni: `tests/test_position_store.py`

- [ ] **Step 1: Failing testleri yaz**

`tests/test_position_store.py` oluştur:

```python
"""tests/test_position_store.py — sell_position() testleri."""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import pytest
from unittest.mock import patch, MagicMock


def _open_pos(action="YES", shares=71.43, bid=0.90):
    return {
        "position_id": "pos-sell-001", "asset": "BTC", "action": action,
        "slug": "btc-up-5m-test", "pm_entry_price": 0.35,
        "yes_token_id": "yes-tok-111", "no_token_id": "no-tok-222",
        "shares": shares, "current_bid": bid,
    }


@pytest.mark.asyncio
async def test_sell_position_yes_uses_yes_token():
    """YES pozisyon → yes_token_id kullanılır."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "price": "0.90", "sizeFilled": "71.43",
    }
    with patch("execution.position_store.get_client", return_value=fake_client):
        from execution.position_store import sell_position
        await sell_position(_open_pos("YES"))
    call = fake_client.create_and_post_order.call_args[0][0]
    assert call["token_id"] == "yes-tok-111"
    assert call["side"] == "SELL"


@pytest.mark.asyncio
async def test_sell_position_no_uses_no_token():
    """NO pozisyon → no_token_id kullanılır."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "price": "0.10", "sizeFilled": "71.43",
    }
    with patch("execution.position_store.get_client", return_value=fake_client):
        from execution.position_store import sell_position
        await sell_position(_open_pos("NO"))
    call = fake_client.create_and_post_order.call_args[0][0]
    assert call["token_id"] == "no-tok-222"


@pytest.mark.asyncio
async def test_sell_position_returns_fill_price():
    """Başarılı sell → float fill fiyatı döner."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.return_value = {
        "status": "MATCHED", "price": "0.92", "sizeFilled": "71.43",
    }
    with patch("execution.position_store.get_client", return_value=fake_client):
        from execution.position_store import sell_position
        fill = await sell_position(_open_pos("YES"))
    assert abs(fill - 0.92) < 0.001


@pytest.mark.asyncio
async def test_sell_position_returns_fallback_on_failure():
    """Order başarısız → current_bid fallback döner."""
    fake_client = MagicMock()
    fake_client.create_and_post_order.side_effect = Exception("API down")
    pos = _open_pos("YES", bid=0.87)
    with patch("execution.position_store.get_client", return_value=fake_client):
        from execution.position_store import sell_position
        fill = await sell_position(pos)
    assert abs(fill - 0.87) < 0.001
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
pytest tests/test_position_store.py -v
```

Beklenen: `ImportError: cannot import name 'sell_position'`

- [ ] **Step 3: execution/position_store.py oluştur**

```python
"""execution/position_store.py — SELL order gönder, fill fiyatını döndür."""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from execution.clob_client import get_client


async def sell_position(pos: dict) -> float:
    """
    Açık pozisyonun token'larını satar.

    pos: position dict — action, yes_token_id, no_token_id, shares, current_bid gerekli
    Döner: fill fiyatı (float). API başarısızsa current_bid fallback.
    """
    action   = pos["action"]
    token_id = pos["yes_token_id"] if action == "YES" else pos["no_token_id"]
    shares   = pos.get("shares", 0)
    fallback = pos.get("current_bid", 0.0)

    if not token_id or not shares:
        print(f"[sell] {pos.get('slug')}: token_id veya shares eksik")
        return fallback

    order_args = {
        "token_id":      token_id,
        "price":         fallback,   # piyasa bid'i — IOC ile hızlı dolar
        "size":          shares,
        "side":          "SELL",
        "time_in_force": "IOC",
    }

    try:
        client = get_client()
        resp   = client.create_and_post_order(order_args)
    except Exception as e:
        print(f"[sell] {pos.get('slug')}: SELL hatası — {e}, fallback={fallback:.4f}")
        return fallback

    if not resp:
        return fallback

    status    = resp.get("status") if isinstance(resp, dict) else getattr(resp, "status", "")
    price_str = resp.get("price")  if isinstance(resp, dict) else getattr(resp, "price", None)

    if status == "MATCHED" and price_str:
        return float(price_str)

    print(f"[sell] {pos.get('slug')}: SELL UNMATCHED (status={status}), fallback kullanılıyor")
    return fallback
```

- [ ] **Step 4: Testleri çalıştır — 4 PASS bekliyoruz**

```bash
pytest tests/test_position_store.py -v
```

Beklenen: 4 PASSED

- [ ] **Step 5: Tam test suite**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 4 yeni PASSED, 0 FAILED

- [ ] **Step 6: Commit**

```bash
git add execution/position_store.py tests/test_position_store.py
git commit -m "feat(execution): position_store — sell_position CLOB SELL order"
```

---

## Task 7: main_loop.py — Router + Exit + BANKROLL_USD env

**Dosyalar:**
- Güncelle: `main_loop.py`
- Güncelle: `tests/test_main_loop.py`

### Step 1: Failing testleri yaz

`tests/test_main_loop.py` sonuna ekle:

```python
# ── Task: CLOB executor router ────────────────────────────────────────────────

def test_bankroll_reads_from_env(monkeypatch):
    """BANKROLL_USD env değişkeni set edilince main_loop bu değeri kullanır."""
    monkeypatch.setenv("BANKROLL_USD", "50.0")
    import importlib
    import main_loop as ml
    importlib.reload(ml)
    assert abs(ml.BANKROLL_USD - 50.0) < 0.01
    importlib.reload(ml)  # Temizle — diğer testleri etkilemesin


@pytest.mark.asyncio
async def test_live_monitor_calls_sell_position_on_exit():
    """DRY_RUN=False iken _monitor_positions çıkışta sell_position çağırır."""
    import config
    from datetime import datetime, timedelta, timezone
    opened_at = (datetime.now(timezone.utc) - timedelta(minutes=5)).isoformat()
    pos = {
        "position_id": "live-pos-001", "asset": "BTC", "action": "YES",
        "slug": "btc-up-test", "pm_entry_price": 0.35, "fair_value": 0.55,
        "ref_price": 95000.0, "position_usd": 25.0, "kelly_f": 0.15,
        "confidence_score": 82.5, "seconds_remaining": 900,
        "opened_at": opened_at, "status": "open",
        "requires_human_approval": False, "dry_run": False,
        "exit_reason": None, "closed_at": None,
        "shares": 71.43, "order_id": "ord-001",
        "yes_token_id": "yes-tok-111", "no_token_id": "no-tok-222",
    }
    open_pos = [pos]
    closed   = []
    fake_window = {"best_ask": 0.92, "best_bid": 0.90,
                   "seconds_remaining": 900, "neg_risk": False}

    with patch.object(config, "DRY_RUN", False), \
         patch("main_loop.current_price",      new_callable=AsyncMock, return_value=95000.0), \
         patch("main_loop.fetch_by_slug",      new_callable=AsyncMock, return_value={}), \
         patch("main_loop.parse_market_window", return_value=fake_window), \
         patch("main_loop.check_exit",          return_value="profit_target_hit"), \
         patch("main_loop.sell_position",       new_callable=AsyncMock, return_value=0.91) as mock_sell:
        await _monitor_positions(open_pos, closed)

    mock_sell.assert_called_once()
    assert len(closed) == 1
    assert abs(closed[0]["pm_exit_price"] - 0.91) < 0.001
```

- [ ] **Step 2: Testleri çalıştır — FAIL bekliyoruz**

```bash
pytest tests/test_main_loop.py::test_bankroll_reads_from_env \
       tests/test_main_loop.py::test_live_monitor_calls_sell_position_on_exit -v
```

Beklenen: FAIL

- [ ] **Step 3: main_loop.py import bloğunu güncelle**

Mevcut `from execution.executor import execute` satırını **SİL**, yerine şunları ekle:

```python
import os
from execution.executor       import execute as _dry_execute
from execution.clob_executor  import execute as _clob_execute
from execution.position_store import sell_position
```

`BANKROLL_USD` sabitini güncelle:

```python
# Eski:
BANKROLL_USD = 1000.0

# Yeni:
BANKROLL_USD = float(os.getenv("BANKROLL_USD", "1000.0"))
```

- [ ] **Step 4: main_loop.py — execute() router fonksiyonu ekle**

`BANKROLL_USD` sabitinin hemen altına yeni `execute` router fonksiyonunu ekle.
Mevcut `await execute(...)` çağrıları değişmez — sadece bu fonksiyon tanımı eklenir:

```python
async def execute(finding, gate_result, risk_result, open_positions):
    """DRY_RUN flag'ine göre doğru executor'a yönlendir. Runtime'da değerlendirilir."""
    if config.DRY_RUN:
        return await _dry_execute(finding, gate_result, risk_result, open_positions)
    return await _clob_execute(finding, gate_result, risk_result, open_positions)
```

Bu yaklaşım mevcut testleri kırmaz: testler zaten `main_loop.execute`'u mock'luyor,
router fonksiyon aynı isimde olduğu için mock'lar çalışmaya devam eder.

- [ ] **Step 6: main_loop.py — _monitor_positions içine sell path ekle**

`_monitor_positions` fonksiyonundaki exit_reason bloğunu bul:

```python
            if exit_reason:
                # NO: 1 - YES_ask = NO bid (sattığımız fiyat). YES: doğrudan YES ask.
                if pos["action"] == "NO":
                    pm_exit = round(1 - window["best_ask"], 4)
                else:
                    pm_exit = window["best_ask"]
```

Şu şekilde değiştir:

```python
            if exit_reason:
                if config.DRY_RUN:
                    # DRY_RUN: modelled fiyat
                    if pos["action"] == "NO":
                        pm_exit = round(1 - window["best_ask"], 4)
                    else:
                        pm_exit = window["best_ask"]
                else:
                    # LIVE: gerçek SELL order — token'ları sat, fill fiyatını al
                    pos["current_bid"] = round(1 - window["best_ask"], 4) if pos["action"] == "NO" else window["best_bid"]
                    pm_exit = await sell_position(pos)
```

- [ ] **Step 7: Testleri çalıştır — 2 PASS bekliyoruz**

```bash
pytest tests/test_main_loop.py::test_bankroll_reads_from_env \
       tests/test_main_loop.py::test_live_monitor_calls_sell_position_on_exit -v
```

Beklenen: 2 PASSED

- [ ] **Step 8: Tam test suite — sıfır regresyon**

```bash
pytest tests/ -q --tb=short
```

Beklenen: önceki sayı + 2 yeni PASSED, 0 FAILED

- [ ] **Step 9: Commit + push**

```bash
git add main_loop.py tests/test_main_loop.py
git commit -m "feat(main_loop): CLOB executor router + sell path + BANKROLL_USD env"
git push
```

---

## Task 8: Aşama 1 Bağlantı Testi (Connection Canary)

**Dosyalar:**
- Yeni: `analysis/clob_connection_test.py`

Bu script `.env`'deki credentials ile Polymarket CLOB API'ye bağlanır ve bağlantıyı doğrular. Order GÖNDERİLMEZ.

- [ ] **Step 1: analysis/clob_connection_test.py oluştur**

```python
#!/usr/bin/env python3
"""analysis/clob_connection_test.py — Aşama 1: CLOB API bağlantı doğrulama.

Kullanım:
    python analysis/clob_connection_test.py

Kontroller:
  1. Credentials yükle + client başlat
  2. Wallet USDC bakiyesini oku
  3. Bilinen bir BTC marketi için clobTokenIds doğrula
  4. Order GÖNDERİLMEZ

Başarı koşulu: tüm adımlar geçer, sonunda "HAZIR" mesajı.
"""
import asyncio
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from dotenv import load_dotenv
load_dotenv()

from execution.clob_client import get_client, reset_client
from data.shortterm import find_shortterm


async def run_canary() -> bool:
    print("=" * 60)
    print("CLOB API BAĞLANTI TESTİ — Order gönderilmez")
    print("=" * 60)

    # 1. Client başlat
    print("\n[1] Credentials yükleniyor...")
    try:
        reset_client()
        client = get_client()
        print("    ✓ ClobClient oluşturuldu")
    except KeyError as e:
        print(f"    ✗ Eksik env değişkeni: {e}")
        print("      .env dosyasına POLY_PRIVATE_KEY, POLY_API_KEY, POLY_API_SECRET, POLY_API_PASSPHRASE ekle")
        return False
    except Exception as e:
        print(f"    ✗ Client hatası: {e}")
        return False

    # 2. Wallet bakiyesi
    print("\n[2] Wallet bakiyesi okunuyor...")
    try:
        balance = client.get_balance()
        usdc = balance.get("USDC") or balance.get("usdc") or balance
        print(f"    ✓ Bakiye: {usdc}")
    except Exception as e:
        print(f"    ✗ Bakiye okunamadı: {e}")
        print("      Bu hata authentication sorunu işareti olabilir.")
        return False

    # 3. clobTokenIds doğrulama
    print("\n[3] BTC market clobTokenIds okunuyor...")
    try:
        markets = await find_shortterm(intervals=(5,))
        btc_markets = [m for m in markets if "btc" in m.get("slug", "").lower()]
        if btc_markets:
            m = btc_markets[0]
            token_ids = m.get("clobTokenIds", [])
            print(f"    ✓ Market: {m['slug']}")
            print(f"      YES token: {token_ids[0] if token_ids else 'YOK'}")
            print(f"      NO token:  {token_ids[1] if len(token_ids) > 1 else 'YOK'}")
            if not token_ids:
                print("    ⚠ clobTokenIds boş — Polymarket API yanıtını kontrol et")
        else:
            print("    ⚠ Şu an aktif BTC marketi yok (normal olabilir)")
    except Exception as e:
        print(f"    ✗ Market verisi okunamadı: {e}")
        return False

    print("\n" + "=" * 60)
    print("✅ HAZIR — DRY_RUN=False öncesi BANKROLL_USD=50 ayarla (.env'de)")
    print("=" * 60)
    return True


if __name__ == "__main__":
    ok = asyncio.run(run_canary())
    sys.exit(0 if ok else 1)
```

- [ ] **Step 2: .env'e credentials ekle (kullanıcı yapar)**

`.env` dosyasına şu satırları ekle (CLAUDE dokunmaz, kullanıcı el ile yazar):
```
POLY_PRIVATE_KEY=0x...
POLY_WALLET_ADDRESS=0x...
POLY_API_KEY=...
POLY_API_SECRET=...
POLY_API_PASSPHRASE=...
```

Credentials nereden alınır:
- Metamask: Settings → Security → Export private key
- Polymarket API key: polymarket.com → Profil → API → Create API Key

- [ ] **Step 3: Bağlantı testini çalıştır**

```bash
cd /root/mispricing_agent && source venv/bin/activate
python analysis/clob_connection_test.py
```

Beklenen çıktı:
```
[1] Credentials yükleniyor...
    ✓ ClobClient oluşturuldu
[2] Wallet bakiyesi okunuyor...
    ✓ Bakiye: {'USDC': '50.00', ...}
[3] BTC market clobTokenIds okunuyor...
    ✓ Market: btc-updown-5m-...
      YES token: 0x...abc
      NO token:  0x...xyz
✅ HAZIR
```

- [ ] **Step 4: Commit + push**

```bash
git add analysis/clob_connection_test.py
git commit -m "feat(analysis): clob_connection_test — Aşama 1 canary script"
git push
```

---

## Sonraki Adım: Aşama 2 (DRY_RUN=False)

Aşama 1 testi geçtikten sonra:

1. `.env`'e ekle: `BANKROLL_USD=50`
2. Polymarket hesabına USDC transfer et (Polygon ağında, minimum $20-30)
3. `tmux attach -t mispricing` ile bota bağlan
4. Botu durdur (Ctrl+C)
5. **İNSAN ONAYI İLE:** `config.py` içinde `DRY_RUN=False` yap (ANAYASA kuralı)
6. Botu yeniden başlat: `python main_loop.py`
7. Telegram'dan ilk gerçek order'ı izle

---

## Özet

| Task | Dosyalar | Test |
|------|----------|------|
| 1 | requirements.txt | import check |
| 2 | db/schema.py, db/logger.py, main_loop.py | 2 test |
| 3 | council/scout.py | 2 test |
| 4 | execution/clob_client.py | 3 test |
| 5 | execution/clob_executor.py | 4 test |
| 6 | execution/position_store.py | 4 test |
| 7 | main_loop.py | 2 test |
| 8 | analysis/clob_connection_test.py | canary script |
